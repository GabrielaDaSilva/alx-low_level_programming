#include "main.h"

/**
 * test that the function positive_or_negative()
 * gives the correct output if given a case of 0.
 * Return: Always 0.
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i)
	return (0)
}
